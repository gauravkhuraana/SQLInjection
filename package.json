{
  "name": "sql-injection-demo",
  "version": "1.1.0",
  "description": "A tiny static web app to demo SQL Injection (and its fix) in 1-2 minutes during a presentation. Runs entirely in the browser via sql.js.",
  "private": true,
  "scripts": {
    "start": "npx --yes http-server public -c-1 -p 3000",
    "serve": "npx --yes http-server public -c-1 -p 3000"
  },
  "keywords": ["sql-injection", "security", "demo", "education", "sqli"],
  "license": "MIT"
}
